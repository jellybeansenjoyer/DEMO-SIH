<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    <group android:checkableBehavior="single">
        <item android:id="@+id/dashboard_item"
            android:icon="@drawable/ic_home"
            android:title="@string/dashboard"/>
        <item android:id="@+id/account_item"
            android:icon="@drawable/ic_person"
            android:title="@string/account"/>
    </group>
    <item android:title="@string/pages">
        <menu>
            <item android:id="@+id/login_item"
                android:icon="@drawable/ic_login"
                android:title="@string/login_menu"/>
            <item android:id="@+id/register_item"
                android:icon="@drawable/ic_register"
                android:title="@string/register"/>
            <item android:id="@+id/error_item"
                android:icon="@drawable/ic_error"
                android:title="@string/error"/>

        </menu>
    </item>
</menu>